# 2026-07-19: Add product_detail_id to inventory API response

## Changes

### Domain (`src/domain/inventory.go`)
- Added `ProductDetailID string` field to `Inventory` struct with JSON tag `product_detail_id`

### Repository (`src/database/inventory_repo.go`)
- Updated `scanInventory()` to scan `product_detail_id` from query results (column added between `product_price_id` and `product_id`)

### Database functions (`db/migrations/008_create_functions.sql`)
- Updated `get_inventory_by_id`, `get_inventory_by_price_id`, and `list_inventories` SQL functions to:
  - Return `product_detail_id UUID` in the RETURNS TABLE definition
  - SELECT `pr.product_detail_id` via JOIN with `product_prices`
  - Add `pr.product_detail_id` to GROUP BY clause

### Also fixed
- Fixed database inconsistency where `list_inventories()` function was missing `product_id` column (old version of migration 008 applied to DB). Re-applied inventory functions with DROP/CREATE to match the migration file.
- Added `InventoryID *string` to `ProductPrice` domain and updated price SQL functions (from previous task).
