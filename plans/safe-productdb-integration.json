{"topic":"safe-productdb-integration","scope":"Run integration tests against DATABASE_URL=productdb without touching public data","steps":["Use a random schema with search_path on every test pool connection","Run migrations and all fixtures inside that schema","Reset only the isolated schema between tests and verify the public root sentinel","Remove TEST_DATABASE_URL and destructive table cleanup from test setup","Update documentation and shared change log"]}
