{"topic":"ci-productdb-database-url","scope":"Repair the GitHub Actions integration job so it provisions and explicitly targets productdb while retaining the integration safety guard","steps":["Change only the integration service database name and test-job DATABASE_URL in the backend workflow","Add static workflow regression coverage for productdb and the absence of TEST_DATABASE_URL","Document the CI contract in the backend README and record the decision"]}
