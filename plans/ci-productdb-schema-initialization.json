{"topic":"ci-productdb-schema-initialization","scope":"Initialize the disposable productdb schema in GitHub Actions before productdb integration tests","steps":["Expose the existing embedded golang-migrate runner through a non-server migrate command","Run that command in the integration job with DATABASE_URL targeting productdb before go test","Add static workflow ordering coverage and update the shared documentation log","Verify focused backend tests, the broader backend test suite, and shared docs generation"]}
